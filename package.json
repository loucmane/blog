{
  "name": "minniewinnie-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm --filter @minniewinnie/ui run build:types && pnpm --filter web exec tsc --noEmit",
    "test:capability": "node scripts/ci/check-test-capability.mjs",
    "ci:taskmaster": "node scripts/ci/check-taskmaster.mjs",
    "ci:aegis": "python3 scripts/ci/check-aegis.py",
    "ci:guard": "python3 scripts/ci/check-legacy-guard.py"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "prettier": "^3.1.0"
  },
  "dependencies": {
    "framer-motion": "^12.15.0"
  }
}
