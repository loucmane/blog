# Task ID: 68

**Title:** Repair Task 39 Capability Script Contract

**Status:** done

**Dependencies:** 39 ✓

**Priority:** high

**Description:** Make the merged root test:capability command execute the complete deterministic unit and browser capability checks.

**Details:**

Replace the argumentless root script with an executable command that runs both required modes, add a regression contract that fails if either mode is omitted, preserve every existing test capability and CI trust boundary, and make no package-version, lockfile, product, workflow, Aegis-runtime, authority, deployment, or secret changes.

**Test Strategy:**

No test strategy provided.
