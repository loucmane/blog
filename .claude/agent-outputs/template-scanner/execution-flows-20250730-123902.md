# Template System Execution Flows

**Generated**: 2025-07-30 12:39:02
**Purpose**: Visual representation of handler execution paths

## Core Execution Flows

### 1. ULTRATHINK Resolution Flow

```mermaid
graph TD
    Start([User Request]) --> Detect{Development Signal?}
    
    Detect -->|Yes| Ultra[Output ULTRATHINK]
    Detect -->|No| Natural[Natural Response]
    
    Ultra --> Parse[Parse S:W:H:E]
    
    Parse --> CheckS{S Valid?}
    CheckS -->|No| ResolveS[resolve-session-void]
    CheckS -->|Yes| CheckW{W Valid?}
    
    ResolveS --> ValidS[Get Valid Session]
    ValidS --> CheckW
    
    CheckW -->|No| ResolveW[resolve-work-void]
    CheckW -->|Yes| CheckH{H Valid?}
    
    ResolveW --> ValidW[Get Valid Work]
    ValidW --> CheckH
    
    CheckH -->|No| ResolveH[resolve-handler-void]
    CheckH -->|Yes| Execute[Execute Handler]
    
    ResolveH --> ValidH[Get Valid Handler]
    ValidH --> Execute
    
    Execute --> Complete([Handler Complete])
```

### 2. Development Work Flow

```mermaid
graph LR
    subgraph "Entry Points"
        A1[work on X]
        A2[implement Y]
        A3[build Z]
    end
    
    subgraph "Initial Routing"
        B[start-new-work]
        C[continue-work]
    end
    
    subgraph "Setup Phase"
        D[create-work-folder]
        E[TodoWrite]
        F[update-session]
    end
    
    subgraph "Development Phase"
        G[standard-dev-workflow]
        H[create-component]
        I[refactor-code]
    end
    
    subgraph "Support Tools"
        J[search-code]
        K[edit-file]
        L[commit-changes]
    end
    
    A1 --> B
    A2 --> B
    A3 --> B
    
    B --> D
    D --> E
    E --> F
    F --> G
    
    G --> J
    G --> K
    G --> L
    
    H --> J
    H --> K
    I --> J
    I --> K
```

### 3. Search and Analysis Flow

```mermaid
graph TD
    subgraph "Search Triggers"
        S1[find X]
        S2[search for Y]
        S3[where is Z]
        S4[how does X work]
    end
    
    subgraph "Pattern Routing"
        P1[tool-selection pattern]
        P2[evidence-check pattern]
    end
    
    subgraph "Tool Handlers"
        T1[search-code]
        T2[find-symbol]
        T3[find-references]
        T4[grep-pattern]
    end
    
    subgraph "Actual Tools"
        A1[Serena find_symbol]
        A2[Serena search_pattern]
        A3[Grep]
        A4[Task agent]
    end
    
    S1 --> P1
    S2 --> P1
    S3 --> P1
    S4 --> P2
    
    P1 --> T1
    P1 --> T2
    P2 --> T1
    
    T1 -->|Code symbols| A1
    T1 -->|Text patterns| A2
    T2 --> A1
    T3 --> A1
    T4 -->|Simple text| A3
    T1 -->|Complex search| A4
```

### 4. File Operation Flow

```mermaid
sequenceDiagram
    participant User
    participant Pattern
    participant Handler
    participant Convention
    participant Tool
    
    User->>Pattern: "edit tracker.md"
    Pattern->>Pattern: file-operation pattern
    Pattern->>Convention: Check special file rules
    Convention-->>Pattern: Append-only rule
    Pattern->>Handler: edit-file handler
    Handler->>Tool: Read file first
    Tool-->>Handler: Content
    Handler->>Convention: Check edit rules
    Convention-->>Handler: Validated
    Handler->>Tool: Edit with constraints
    Tool-->>User: Success
```

### 5. Git Operations Flow

```mermaid
graph TD
    subgraph "Git Triggers"
        G1[commit changes]
        G2[save work]
        G3[create PR]
    end
    
    subgraph "Validation"
        V1[check-commit-msg]
        V2[gather-evidence]
        V3[check-conventions]
    end
    
    subgraph "Execution"
        E1[commit-changes]
        E2[create-branch]
        E3[push-changes]
    end
    
    G1 --> V1
    V1 --> V3
    V3 --> E1
    
    G2 --> V1
    V1 --> E1
    
    G3 --> V2
    V2 --> E2
    E2 --> E3
```

### 6. Testing Flow

```mermaid
graph LR
    subgraph "Test Triggers"
        T1[test X]
        T2[validate Y]
        T3[check Z works]
    end
    
    subgraph "Test Handlers"
        H1[create-test-checkpoint]
        H2[simulation-test]
        H3[validate-changes]
    end
    
    subgraph "User Interaction"
        U1[Create checkpoint]
        U2[User tests]
        U3[Report results]
    end
    
    T1 --> H1
    T2 --> H3
    T3 --> H3
    
    H1 --> U1
    U1 --> U2
    U2 --> U3
    
    H2 --> U1
    H3 --> U2
```

### 7. Session Management Flow

```mermaid
stateDiagram-v2
    [*] --> NoSession
    
    NoSession --> SessionStart: start-session
    SessionStart --> ActiveSession: SESSION.md created
    
    ActiveSession --> Working: start-new-work
    Working --> Checkpoint: update-session
    Checkpoint --> Working: continue
    
    Working --> EndSession: end-session
    EndSession --> Memory: Create Serena memory
    Memory --> [*]
    
    ActiveSession --> Compaction: Context full
    Compaction --> NewSession: New SESSION.md
    NewSession --> ActiveSession: Continue work
```

### 8. Error Recovery Flow

```mermaid
graph TD
    subgraph "Error Detection"
        E1[Handler not found]
        E2[File not found]
        E3[Symbol not found]
        E4[Test failure]
    end
    
    subgraph "Recovery Matrix"
        M[error-recovery-matrix]
    end
    
    subgraph "Recovery Actions"
        R1[Search broader terms]
        R2[Verify path]
        R3[Try substring match]
        R4[Read full error]
    end
    
    subgraph "Fallback"
        F1[Check PATTERNS.md]
        F2[Ask user]
        F3[Create missing]
    end
    
    E1 --> M
    E2 --> M
    E3 --> M
    E4 --> M
    
    M --> R1
    M --> R2
    M --> R3
    M --> R4
    
    R1 --> F1
    R2 --> F2
    R3 --> F2
    R4 --> F3
```

## Handler Dependency Chains

### Development Chain
```
User Input
└── start-new-work
    ├── create-work-folder
    │   └── Write (7 files)
    ├── TodoWrite
    │   └── Task breakdown
    ├── update-session
    │   └── Edit SESSION.md
    └── standard-dev-workflow
        ├── search-code
        ├── edit-file
        ├── create-test-checkpoint
        └── commit-changes
```

### Search Chain
```
User Query
└── tool-selection (pattern)
    ├── search-code (handler)
    │   ├── Serena find_symbol
    │   ├── Serena search_pattern
    │   └── Task agent (complex)
    ├── find-symbol (handler)
    │   └── Serena find_symbol
    └── grep-pattern (handler)
        └── Grep tool
```

### Convention Chain
```
Any Action
└── check-conventions-first
    ├── File conventions
    │   └── Special file rules
    ├── Naming conventions
    │   └── check-naming
    ├── Git conventions
    │   └── check-commit-msg
    └── Code conventions
        └── check-style
```

## Critical Decision Points

### 1. Handler Selection (H = VOID)
```mermaid
graph TD
    A[User Request] --> B{Extract Keywords}
    B --> C[Search REGISTRY]
    C --> D{Multiple Matches?}
    
    D -->|Yes| E[Score by Relevance]
    D -->|No| F{Any Match?}
    
    E --> G[Select Best]
    F -->|Yes| G
    F -->|No| H[Try Patterns]
    
    H --> I{Pattern Match?}
    I -->|Yes| J[Use Pattern Handler]
    I -->|No| K[Ask User]
    
    G --> L[Valid Handler]
    J --> L
    K --> L
```

### 2. Tool Selection
```mermaid
graph TD
    A[Need Tool] --> B{Operation Type?}
    
    B -->|Search| C{What For?}
    B -->|Edit| D{File Type?}
    B -->|Execute| E{Command Type?}
    
    C -->|Code Symbol| F[Serena find_symbol]
    C -->|Text Pattern| G[Serena search_pattern]
    C -->|Simple Text| H[Grep]
    
    D -->|Any File| I[Edit/MultiEdit]
    D -->|New File| J[Write]
    
    E -->|Shell| K[Bash]
    E -->|Git| L[gac alias]
```

### 3. Work Context (W = VOID)
```mermaid
graph TD
    A[Request] --> B{Analyze Domain}
    
    B -->|Implementation| C[Check Active Folders]
    B -->|Bug/Fix| D[Check Bug Folders]
    B -->|Search| E[W = investigating]
    B -->|Review| F[W = reviewing]
    
    C --> G{Match Found?}
    D --> G
    
    G -->|Yes| H[W = folder-name]
    G -->|No| I[Route to start-new-work]
    
    E --> J[Valid W]
    F --> J
    H --> J
    I --> J
```

## System Entry Points Summary

1. **Primary Entry**: ULTRATHINK → Handler Selection → Execution
2. **Pattern Entry**: Ambiguous Request → Pattern → Handler
3. **Direct Entry**: Clear Trigger → Specific Handler
4. **Error Entry**: Failure → Recovery Matrix → Fallback
5. **Meta Entry**: System Improvement → Building Better

---

**End of Execution Flow Documentation**